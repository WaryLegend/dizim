apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appName }}-strapi
  labels:
    app: {{ .Values.appName }}
    component: strapi
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 1337
      name: http
  selector:
    app: {{ .Values.appName }}
    component: strapi
